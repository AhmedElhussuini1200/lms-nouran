services:
  evolution:
    image: atendize/evolution-api:v2
    restart: unless-stopped
    ports:
      - "8080:8080"
    environment:
      - AUTHENTICATION_API_KEY=lms-evo-key-CHANGE-ME
      - DATABASE_ENABLED=false
    volumes:
      - ./evolution-data:/evolution/instances
